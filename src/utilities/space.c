#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include <ctype.h>
#include <string.h>

void print(char *string){
	// // // // printf("%s", string);
}

void printDivider(){
	print("///////////////////////////////////");
}

void printTitle(char *string){
	// printf("fdfdfdfdfdfdf%s", string);
	// for (size_t i = 0; i < (strlen(string) + 1); i++){
	// 	*(string + i) = toupper(*(string + i));
	// }
	// printf("%s", string);
}

void messageStart(void) {
	// // // // printf("\n\n///////////////////////////////////\n\n\n");
	// // // // printf("\nGame is running!!!\n\n");
}
void messageEnd(void) {
	// // // // printf("\n\n\n///////////////////////////////////\n\n");
	// // // // printf("\n\n\n");
}