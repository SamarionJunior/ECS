#ifndef EMPTY_MATRIX_H_
#define EMPTY_MATRIX_H_

#include <stdio.h>
#include <stdlib.h>
#include <stdbool.h>
#include "matrix.h"

bool emptyRow(Matrix* matrix, size_t row);
bool emptyMatrix(Matrix* matrix);

#endif

